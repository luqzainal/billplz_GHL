{
  "name": "billplz-payment",
  "version": "1.0.0",
  "description": "Billplz Payment Integration",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd server && npm install && cd ../client && npm install && npm run build",
    "dev": "nodemon server.js"
  },
  "keywords": [
    "payment",
    "billplz"
  ],
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mongoose": "^8.1.3",
    "dotenv": "^16.4.5",
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
} 